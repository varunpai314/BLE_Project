name: ble_server
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.10.8

# Add regular dependencies here.
dependencies:
  dotenv: ^4.2.0
  firebase_admin_sdk: ^0.5.1
  google_cloud_firestore: ^0.5.1
  googleapis_auth: ^2.3.0
  http: ^1.6.0
  path: ^1.9.0
  shelf: ^1.4.2
  shelf_router: ^1.1.4

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
